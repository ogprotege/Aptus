# Aptus
An automated fine-tuning orchestration tool that cuts the most time-consuming stage of LLM training. Input your hardware, data, and targets; it dynamically determines optimal quantization, precision, and VRAM strategies, outputting fully configured, ready-to-execute Python scripts and pipelines.
