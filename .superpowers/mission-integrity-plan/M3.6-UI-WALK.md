# M3.6 Workbench path walk — 2026-08-12

## Method

- Served workbench from `aptus serve --host 127.0.0.1 --port 8787` on main after M4 merge.
- Browser automation against loopback URL with session token.
- Path Alpha identity fields exercised on Facts stage; host scan applied Apple Silicon shared-memory messaging and filled VRAM/host RAM/disk.
- Workflow rail stages clicked: Facts → Compare → Compile → Validate → Run (and back).

## Findings

| Check | Result |
| --- | --- |
| Workbench loads | Pass |
| Five-stage rail present | Pass |
| Facts form (model/dataset/hardware/target) | Pass — fillable |
| Scan this Aptus host | Pass — Metal working-set / unified memory messaging |
| Stage navigation | Pass — no dead ends |
| Labeled example | Pass — marks stages Complete without claiming real execution (honest labeling) |
| P1 UX blockers for Path Alpha | **None found** |

## Scope honesty

A full UI-driven measured train to `measured-run-pass` was **not** repeated here.
Path Alpha measured evidence remains the two CLI ladders in
`docs/operations/evidence/2026-08-12-path-alpha-mlx-m3/`. M3.6 verifies the
workbench surface can carry Journey A without UI dead ends.

## Follow-ups (non-blocking)

- Optional later: one full UI-managed Path Alpha ladder for operator demo footage.
