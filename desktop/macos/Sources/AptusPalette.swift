import AppKit

enum AptusPalette {
    static let brandTeal = NSColor(
        srgbRed: 0x0C / 255,
        green: 0x6E / 255,
        blue: 0x77 / 255,
        alpha: 1
    )
    static let cloud = NSColor.windowBackgroundColor
    static let porcelain = NSColor.controlBackgroundColor
    static let graphite = NSColor.labelColor
    static let circuitTeal = NSColor.controlAccentColor
    static let faultRed = NSColor.systemRed
    static let mutedGraphite = NSColor.secondaryLabelColor
    static let hairline = NSColor.separatorColor
}
