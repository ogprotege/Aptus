# Aptus 0.2 cut note

> **Status:** Active | **Authority:** Owner product call (subordinate to claim language) | **Applies to:** Aptus 0.2 after Lane 3 | **Last reviewed:** 2026-08-18 | **Next scheduled review:** When a later increment is opened, or if this call is superseded

This note records one product call. It is not a schema. There is no
`aptus.product-cut.v1`.

## Call

**Cut-freeze 0.2 as the five-stage referee.**

Aptus 0.2 is Facts → Compare → Compile → Validate → Run, plus the
operator-attested last call after a completed train. That line is frozen as
the referee. Do not grow 0.2. Later work is a new increment, not more 0.2.

This is not a 0.2 ship of model quality. This is not “Aptus decided the
adapters are good.” This is not stop-the-product.

## Operational evidence

Cite only parent validation state and recorded last calls. Do not cite
training loss or gold scores as quality.

| Journey | Job | Parent state | Last call |
| --- | --- | --- | --- |
| A (Path Alpha test-drive) | `job_54c068b57dc34c269f14ba525d755bc4` | `measured-run-pass` | `done` (operator-attested) |
| B (magisterium 7B) | `job_87419924d0f941a580d83979fb9b0f9f` | `measured-run-pass` | `done` (operator-attested) |

Journey A remains the reviewed Path Alpha 0.5B identity. Journey B is a
completed train on an unreviewed 28-layer runtime. It is not Path Alpha
and not a reviewed 7B identity.

`done` closes those recipes. It leaves the files. It does not ship Aptus
0.2. `measured-run-pass` is not Use.

Lane 3 (PR #99, `398b462`) is the last-call door on this frozen line.

## What this does not decide

- Adapter quality
- Whether to load either adapter
- Whether gold exact-match would have been Use or Stop
- Chrome, AutoML, more epochs, or a third journey on the same recipes
